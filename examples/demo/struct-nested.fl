struct Point {
  x ∈ Real,
  y ∈ Real
} →

struct Rectangle {
  origin ∈ Point,
  width ∈ Real,
  height ∈ Real
} →

theorem RectangleOriginProjection() {
  assume(r ∈ Rectangle) →
  declareToProve(r.origin ∈ Point)
} ↔

proof RectangleOriginProjection() {
  conclude(r.origin ∈ Point)
}
