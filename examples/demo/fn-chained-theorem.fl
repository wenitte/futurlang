fn double(n ∈ Nat) -> Nat {
  conclude(n + n)
} →

theorem DoubleStable() {
  assume(n ∈ Nat) →
  declareToProve(double(n) -> double(n))
} ↔

proof DoubleStable() {
  assume(double(n)) →
  conclude(double(n))
}
