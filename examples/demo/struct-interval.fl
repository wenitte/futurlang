struct Interval {
  lo ∈ Real,
  hi ∈ Real
} →

theorem IntervalFieldProjection() {
  assume(i ∈ Interval) →
  declareToProve(i.lo ∈ Real)
} ↔

proof IntervalFieldProjection() {
  conclude(i.lo ∈ Real)
}
