type Result =
  | Ok(v ∈ Nat)
  | Err(msg ∈ String)
} →

theorem ResultValid() {
  assume(r ∈ Result) →
  declareToProve(r ∈ Result)
} ↔

proof ResultValid() {
  match r {
    case Ok(v) => conclude(r ∈ Result)
    case Err(_) => conclude(r ∈ Result)
  }
}
