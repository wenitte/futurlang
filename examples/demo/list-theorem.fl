theorem ListCaseProof() {
  assume(xs ∈ List(Nat)) →
  declareToProve(xs ∈ List(Nat))
} ↔

proof ListCaseProof() {
  match xs {
    case [] => conclude(xs ∈ List(Nat))
    case [x, ...rest] => conclude(xs ∈ List(Nat))
  }
}
