struct Point {
  x ∈ Real,
  y ∈ Real
} →

fn xCoord(p ∈ Point) -> Real {
  conclude(p.x)
} →

theorem XCoordStable() {
  given(p ∈ Point) →
  assert(xCoord(p) -> xCoord(p))
} ↔

proof XCoordStable() {
  assume(xCoord(p)) →
  conclude(xCoord(p))
}
