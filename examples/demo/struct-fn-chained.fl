struct Point {
  x ∈ Real,
  y ∈ Real
} →

fn xCoord(p ∈ Point) -> Real {
  conclude(p.x)
} →

theorem XCoordStable() {
  assume(p ∈ Point) →
  declareToProve(xCoord(p) -> xCoord(p))
} ↔

proof XCoordStable() {
  assume(xCoord(p)) →
  conclude(xCoord(p))
}
