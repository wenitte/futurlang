theorem ProbabilityComplement() {
  assume(ProbabilityMeasure(P, Σ, X) ∧ A ∈ Σ) →
  declareToProve(P(complement(A, X)) = 1 - P(A))
} ↔

proof ProbabilityComplement() {
  conclude(P(complement(A, X)) = 1 - P(A))
}
