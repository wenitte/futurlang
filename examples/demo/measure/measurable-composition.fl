theorem MeasurableComposition() {
  assume(Measurable(f, Σ_X, Σ_Y) ∧ Measurable(g, Σ_Y, Σ_Z)) →
  declareToProve(Measurable(g ∘ f, Σ_X, Σ_Z))
} ↔

proof MeasurableComposition() {
  conclude(Measurable(g ∘ f, Σ_X, Σ_Z))
}
