type Shape =
  | Circle(r ∈ Real)
  | Rectangle(w ∈ Real, h ∈ Real)
} →

theorem WildMatch() {
  assume(s ∈ Shape) →
  declareToProve(s ∈ Shape)
} ↔

proof WildMatch() {
  match s {
    case Circle(r) => conclude(s ∈ Shape)
    case _ => conclude(s ∈ Shape)
  }
}
