lemma ForwardStep() {
  assume(p -> q) →
  declareToProve(q)
} ↔

proof ForwardStep() {
  assume(p) →
  conclude(q)
} →

theorem UsesForwardStep() {
  assume(p -> q) →
  declareToProve(q)
} ↔

proof UsesForwardStep() {
  assume(p) →
  apply(ForwardStep)
}
