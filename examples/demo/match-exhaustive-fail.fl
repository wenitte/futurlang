type Shape =
  | Circle(r ∈ Real)
  | Rectangle(w ∈ Real, h ∈ Real)
} →

theorem BadMatch() {
  assume(s ∈ Shape) →
  declareToProve(s ∈ Shape)
} ↔

proof BadMatch() {
  match s {
    case Circle(r) => conclude(s ∈ Shape)
  }
}
