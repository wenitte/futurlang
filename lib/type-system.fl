// type-system.fl — Phase 4: richer dependent types
// Tests Σ-elimination, subtype coercions, and Π-types.

// ── Σ-type projections ────────────────────────────────────────────────────────

// Given a pair p : Σ n ∈ Nat, even(n), we can extract fst(p) ∈ Nat
theorem SigmaFstProj() {
  assume(p ∈ Σ n ∈ Nat, even(n)) →
  declareToProve(fst(p) ∈ Nat)
} ↔

proof SigmaFstProj() {
  conclude(fst(p) ∈ Nat)
} →

// The second component gives us the property at fst(p)
theorem SigmaSndProj() {
  assume(p ∈ Σ n ∈ Nat, even(n)) →
  declareToProve(even(fst(p)))
} ↔

proof SigmaSndProj() {
  conclude(even(fst(p)))
} →

// ── Subtype coercions ─────────────────────────────────────────────────────────

// Every natural number is an integer
theorem NatIsInt() {
  assume(n ∈ Nat) →
  declareToProve(n ∈ Int)
} ↔

proof NatIsInt() {
  conclude(n ∈ Int)
} →

// Every natural number is a real
theorem NatIsReal() {
  assume(n ∈ Nat) →
  declareToProve(n ∈ Real)
} ↔

proof NatIsReal() {
  conclude(n ∈ Real)
} →

// Every integer is a real
theorem IntIsReal() {
  assume(n ∈ Int) →
  declareToProve(n ∈ Real)
} ↔

proof IntIsReal() {
  conclude(n ∈ Real)
} →

// ── Π-type application ────────────────────────────────────────────────────────

// If we know ∀ x ∈ Nat, x + 0 = x, applying it gives us n + 0 = n
theorem PiElimArith() {
  assume(n ∈ Nat ∧ ∀ x ∈ Nat, x + 0 = x) →
  declareToProve(n + 0 = n)
} ↔

proof PiElimArith() {
  conclude(n + 0 = n)
}
